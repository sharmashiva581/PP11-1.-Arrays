import java.io.*;
import java.util.*;

public class Main{
  public static void display(int[] a){
    StringBuilder sb = new StringBuilder();

    for(int val: a){
      sb.append(val + " ");
    }
    System.out.println(sb);
  }

  public static void rotate(int[] a, int k){
 	 	 // Write your code here
 	 	 
 	 	 k= k % a.length; // if k is greater than or equal to  length of array....make k effective
 	 	 
 	 	 // Now this IF will handle the negative cases
 	 	 if(k < 0 ){
 	 	     k = k + a.length;
 	 	 }
 	 	 
 	 	 // 1. reverse complete array 
 	 	 int lo = 0;
 	 	 int hi = a.length-1;
 	 	
 	 	 while(lo < hi){
 	 	     int temp = a[lo];
 	 	     a[lo] = a[hi];
 	 	     a[hi] = temp;
 	 	     lo++;
 	 	     hi--;
 	 	 }
 	 	 // NOW REVERSE 0 TO K-1 PART....make custom reverse functions
 	 	  reverse(a, 0 , k-1);
 	 	  
 	 	   // NOW REVERSE K TO LAST INDEX PART
 	 	  reverse(a , k , a.length-1);
 	}
 	public static void reverse(int a[] , int lo , int hi){
 	    while(lo< hi ){
 	        int temp = a[lo];
 	        a[lo] = a[hi];
 	        a[hi] = temp;
 	        lo++;
 	        hi--;
 	    }
 	}
   
  

public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

    int n = Integer.parseInt(br.readLine());
    int[] a = new int[n];
    for(int i = 0; i < n; i++){
       a[i] = Integer.parseInt(br.readLine());
    }
    int k = Integer.parseInt(br.readLine());

    rotate(a, k);
    display(a);
 }

}
